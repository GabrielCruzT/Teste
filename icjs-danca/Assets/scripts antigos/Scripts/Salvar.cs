using UnityEngine;
using System.Collections;
using UnityEngine.UI;
using UnityEngine.SceneManagement;
using System.IO;

public class Salvar {

	/*public void SalvarDadosTeste(string[] dados, string local)
	{
		if(File.Exists(local))
			File.Delete (local);
		using (StreamWriter sw = File.AppendText(local))
		{
			foreach (string line in dados)
			{
				sw.WriteLine(line);
			}
		}
	}
	public void AdicionarDados(string[] dados,string local, string dadoAdicionado)
	{
		using (StreamWriter sw = File.AppendText(local))
		{
			foreach (string line in dados)
			{
				sw.WriteLine(line);
			}
			sw.WriteLine (dadoAdicionado);
		}
	}
	public void ExcluirDados(string[] dados, string local, string dadoExcluido, bool tipo)
	{

	}*/
}
